(function(){
	const slide = new Slider();

	const slide2 = new Slider({
		ctx : '#slider2'
	});


	/**
	 * Slider
	 *
	 * elem : slider wrapper
	 * elemList : slider list
	 * nextBtn : next btn className
	 * prevBtn : prev btn className
	 *
	 */
})();